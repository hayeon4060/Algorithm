class Solution:
    def search(self, nums: List[int], target: int) -> int:
        
        try:
            found = nums.index(target)
            
        except: 
            found =-1
            
            
        return found
